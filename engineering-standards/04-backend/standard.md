# 后端团队规范

当前暂无 evidence-backed 后端规则。

新增规则必须满足：

1. 来自真实后端项目代码事实，或由后端负责人确认。
2. 规则正文保持团队级抽象。
3. Controller / Service / Repository / DTO / Entity 等具体路径写入 `evidence/`。
4. 无 evidence 规则进入 `pending-confirmation.md`，不得写成 AI 必须遵守的 `draft`。

本文件当前仅为结构占位，不得作为 AI 默认执行规则来源。
