# PC 客户端团队规范

当前暂无 evidence-backed PC 客户端规则。

新增规则必须满足：

1. 来自真实 PC 客户端项目代码事实，或由 PC 客户端负责人确认。
2. 规则正文保持团队级抽象。
3. Electron main / preload / renderer、IPC、原生桥接、自动更新等具体路径写入 `evidence/`。
4. 无 evidence 规则进入 `pending-confirmation.md`，不得写成 AI 必须遵守的 `draft`。

本文件当前仅为结构占位，不得作为 AI 默认执行规则来源。
