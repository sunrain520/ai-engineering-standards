# Batch Extraction Workflow

Use this when a caller wants to run fact collection and mining for a subset of the generated batch plan.

Each batch still writes artifacts under the same `.runs/{run_id}/` boundary.
