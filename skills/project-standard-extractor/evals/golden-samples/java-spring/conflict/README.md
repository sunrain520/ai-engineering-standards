# Conflict Fixture

`OrderController` directly references `OrderMapper`, while the same sample also contains normal Controller and Service anchors. This proves conflict routing without relying on a real enterprise sample to contain a contradiction.
